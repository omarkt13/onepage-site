# onepage-site

Product site for OnePage, the e-ink phone companion. Single self-contained
`index.html` — no build step, no dependencies, inline CSS/JS, light + dark
themes, reduced-motion safe.

Preview: open `index.html` in any browser.
